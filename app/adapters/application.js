import DS from 'ember-data';

export default DS.JSONAPIAdapter.extend({
  // host: 'http://marsweather.ingenology.com',
  // namespace: 'v1/latest',
  // headers: {
  //   Access-Control-Allow-Origin: *
  // }
  // pathForType: function(type) {
  //   return (type);
  // }
});
